apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cat-cozy.fullname" . }}-test-connection"
  labels:
    {{- include "cat-cozy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cat-cozy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
